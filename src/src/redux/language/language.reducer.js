import LanguageActionTypes from './language.types'

const initialState = {
  languages: []
}

export default (state = initialState, action) => {
  switch(action.type) {
    default: 
      return state
  }
}